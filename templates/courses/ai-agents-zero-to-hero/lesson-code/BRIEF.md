---
workflow: general-video
flow: automation
storyboard: yes
message: "M2/M4 build lesson — code reveal. Hook lockup → code panel lines reveal as read → output → takeaway → outro."
destination: all-shorts
aspect: 1728x3072
language: en
length: 30-90s
angle: how-to
---

## Intent

Reusable lesson template for code/build lessons (Modules 2 & 4) of the AI Agents Zero→Hero course. Real code reveals line-by-line as the VO explains, then shows the output. Spec: `spec.md`. Kit: `../shared/`.

## Notes

- Demo snippet (`class Agent:`) proves the primitives — replace with the lesson's real code.
- Hook lockup, corner brand, outro stay fixed across the course.
- Beat times are demo values; real lessons derive them from the VO transcript.
- Brand palette: obsidian `#12141C` + gold `#D4AF37` (persona.yaml) — never per-module palettes.
