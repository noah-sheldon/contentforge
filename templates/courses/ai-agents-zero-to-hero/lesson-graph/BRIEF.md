---
workflow: general-video
flow: automation
storyboard: yes
message: "M3/M4 LangGraph lesson — animated graph. Hook lockup → state + nodes + edges build live → takeaway → outro."
destination: all-shorts
aspect: 1728x3072
language: en
length: 30-90s
angle: architecture
---

## Intent

Reusable lesson template for LangGraph lessons (Modules 3 & 4) of the AI Agents Zero→Hero course. The graph diagram builds itself as the VO explains — state pill, nodes, directed edges with labels. Spec: `spec.md`. Kit: `../shared/`.

## Notes

- Demo graph (THINK → ACT → OBSERVE + repeat loop) proves the primitives — replace with the lesson's real graph.
- Hook lockup, corner brand, outro stay fixed across the course.
- Beat times are demo values; real lessons derive them from the VO transcript.
- Brand palette: obsidian `#12141C` + gold `#D4AF37` (persona.yaml) — never per-module palettes.
